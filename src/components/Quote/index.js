import React from "react";
import { Wrapper, Header } from "./Quote.styles";
const Quote = ({ tweet }) => {
    return;
    <Wrapper>
        <Header>{/* <Img src={tweet.img}></Img> */}</Header>
    </Wrapper>;
};

export default Quote;
